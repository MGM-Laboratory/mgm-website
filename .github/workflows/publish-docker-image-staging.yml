name: Publish Docker Images (staging)

on:
  pull_request:
    branches: ["*"]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  id-token: write
  attestations: write
  security-events: write

jobs:
  changes:
    name: Detect Changes
    uses: ./.github/workflows/detect-changes.yml

  build:
    needs: changes
    # Always invoked, never gated at the job level: a skipped job calling a
    # reusable workflow never evaluates that workflow's jobs, so GitHub
    # posts a single check-run named after this job's own id ("build")
    # instead of the reusable workflow's matrix-expanded names ("Build,
    # scan & push API/Web image"). The `run` input carries the actual skip
    # decision down to that workflow's steps instead.
    uses: ./.github/workflows/publish-docker-image.yml
    with:
      # workflow_dispatch has no PR number, so fall back to the run number.
      tag: pr-${{ github.event.pull_request.number || github.run_number }}
      run: ${{ needs.changes.outputs.docker_api == 'true' || needs.changes.outputs.docker_web == 'true' }}
    secrets: inherit

  gate:
    name: Docker staging gate
    if: always()
    needs: [changes, build]
    runs-on: ubuntu-latest
    steps:
      - name: Require selected checks
        env:
          RESULTS: ${{ toJSON(needs) }}
        run: |
          node -e 'const jobs = JSON.parse(process.env.RESULTS); if (jobs.changes.result !== "success" || Object.values(jobs).some(j => !["success", "skipped"].includes(j.result))) process.exit(1)'
